<h1>Productos:</h1>

<% @products.each do |product| %>
    <%= link_to product_path(product.id) do %>
        <h2><%= product.title %></h2>
        <span><%= product.price %>$</span>
        <%# cuando colocamos en el lenguaje de etiquetas el signo igual es porque necesito mostrar en mi pagina 
        el resultado del comando, cuando no lo hago esto se me proceso por debajo sin una salida por app web %>
    <% end %>
<% end %>